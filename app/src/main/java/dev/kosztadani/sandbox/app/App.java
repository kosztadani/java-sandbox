/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dev.kosztadani.sandbox.app;

import dev.kosztadani.sandbox.list.LinkedList;

import static dev.kosztadani.sandbox.utilities.StringUtils.join;
import static dev.kosztadani.sandbox.utilities.StringUtils.split;
import static dev.kosztadani.sandbox.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
